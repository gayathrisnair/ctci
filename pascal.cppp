#include<iostream>
using namespace std;
int main(){
    cout<<"enter number of rows: ";
    cin>>n;
    for(i=0;i<n/2;i++){
        for(j=0;j<(<n-i);j++)
            cout<<" ";  

        for(k=1;k<=i;k++) 
            cout<<(fact(i)/(fact(k)*fact(i-k)))<<" ";
        cout<<"/n";       

    }
}